# Ajouter dans /etc/caddy/Caddyfile (ou fichier dédié dans /etc/caddy/conf.d/)
#
# Remplacer breach-sim.example.com par le vrai sous-domaine
# (ex : breach-sim.breizhland.eu — ajouter le record DNS A correspondant)

korrig.breizhland.eu {
    reverse_proxy 127.0.0.1:8888
}
