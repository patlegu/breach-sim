version: 2
ethernets:
  eth0:
    dhcp4: false
    addresses:
      - ${ip}/${prefix}
    gateway4: ${gateway}
    nameservers:
      addresses:
        - ${gateway}   # OPNsense fait office de DNS relay
        - 1.1.1.1
